diff --git a/requirements.txt b/requirements.txt
index efdfaf9..26b63cc 100644
--- a/requirements.txt
+++ b/requirements.txt
@@ -9,28 +9,27 @@ scikit-image
 opencv-python-headless
 trimesh
 open3d
-tensorboard
-sb3-contrib
-stable_baselines3
+#tensorboard       # training logging
+#sb3-contrib       # RL training
+#stable_baselines3 # RL training
 shapely==2.0.4
-toppra==0.5.3
+#toppra==0.5.3     # motion planning, CuRobo replaces this
 numpy
-seaborn
-klampt
-pyfqmr
-hdf5plugin
-lightning
-torchvision
-wandb
-voxelmap
-PyMCubes
-pybind11[global]==2.13.6
-netifaces
-pyside2
-rospkg
+#seaborn           # plotting
+#klampt            # motion planning, CuRobo replaces this
+#pyfqmr            # mesh simplification
+#hdf5plugin        # not needed
+#lightning         # ML training
+#torchvision       # ML training
+#wandb             # experiment tracking
+#voxelmap          # custom CUDA kernels replace this
+#PyMCubes          # not needed
+pybind11[global]==2.11.1
+#netifaces         # networking
+#pyside2           # GUI
+#rospkg            # ROS
 hydra-core
-ftfy
-regex
-tqdm
+#ftfy              # NLP
+#regex             # not needed
 termcolor
-PyOpenGL
+#PyOpenGL          # not needed
diff --git a/setup.py b/setup.py
index cdf6a63..9ddbc04 100644
--- a/setup.py
+++ b/setup.py
@@ -1,6 +1,7 @@
-from setuptools import setup
+from setuptools import setup, find_packages
 
 setup(name='shelf_gym',
+      packages=find_packages(include=['shelf_gym', 'shelf_gym.*']),
       version='0.0.3',
       install_requires=['gymnasium', 'pybullet', "tqdm",  "h5py", "attrdict", "scipy", "scikit-image", "imutils",
                         "opencv-python", "trimesh", "open3d", "tensorboard", "sb3-contrib", "stable_baselines3", "wandb",
diff --git a/shelf_gym/environments/shelf_environment.py b/shelf_gym/environments/shelf_environment.py
index 3a5800c..5666cfc 100644
--- a/shelf_gym/environments/shelf_environment.py
+++ b/shelf_gym/environments/shelf_environment.py
@@ -141,6 +141,6 @@ class ShelfEnv(RobotEnv):
 
 
 if __name__ == '__main__':
-    env = ShelfEnv(render=True, show_vis=True, use_ycb=True)
+    env = ShelfEnv(render=True, show_vis=False, use_ycb=True)
     while True:
         env.step_simulation(env.per_step_iterations)
\ No newline at end of file
