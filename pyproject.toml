[build-system]
requires = ["setuptools>=61.0", "torch"]
build-backend = "setuptools.build_meta"

[project]
name = "nbv-planner"
version = "0.1.0"
description = "Autonomous Next-Best-View 3D scanning and inspection planner"
requires-python = ">=3.10"
dependencies = []

[tool.uv.workspace]
exclude = ["third_party/*"]

[tool.pytest.ini_options]
pythonpath = ["."]
